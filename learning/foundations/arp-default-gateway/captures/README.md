# Packet Captures

실제 Lab에서 생성한 `.pcap` / `.pcapng` 파일과 캡처 조건을 저장합니다.

권장 파일명 예시:

- `01-arp-request-reply.pcapng`
- `02-icmp-after-arp.pcapng`
- `03-wrong-gateway-arp-repeat.pcapng`

각 캡처에는 가능하면 사용한 Topology, 시작 조건, 확인하려던 가설을 함께 기록합니다.
